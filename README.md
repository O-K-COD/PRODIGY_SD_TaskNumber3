# Contact Management System (OOP)

An Object-Oriented Command Line Interface (CLI) application in Python to manage a personal address book. Allows users to add, view, search, and delete contacts dynamically during runtime.

## Features
- Object-Oriented design using `Contact` and `ContactManager` classes
- Case-insensitive search and deletion by name
- Interactive command-line menu loop
